version: 1
entries:
  aida:
    kind: aida
    profiles: []

  pam:
    kind: subagent
    call_sign: Pamela
    aliases: [pm]
    description: Product manager persona.
    profiles: [work]
    subagent:
      dir: testdata/lint/dup-alias/dir-a
      agent: pam

  pamela-two:
    kind: subagent
    aliases: [pamela]
    description: A different entry that happens to alias the same call-sign.
    profiles: [work]
    subagent:
      dir: testdata/lint/dup-alias/dir-a
      agent: pam
