Just a stub agent file with no frontmatter block at all, to exercise the
tolerant parser: no "---" delimiters, so discovery should still produce an
entry for this file, just with an empty description.
